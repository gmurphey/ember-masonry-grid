{{#if customLayout}}
  {{yield items this}}
{{else}}
  {{#each items as |item index|}}
    {{#masonry-item grid=this item=item onItemClick=onItemClick}}
      {{yield item index this}}
    {{/masonry-item}}
  {{/each}}
{{/if}}

